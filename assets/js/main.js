const menu=document.querySelector('.menu'),links=document.querySelector('.nav-links');
if(menu){const closeMenu=()=>{links.classList.remove('open');menu.setAttribute('aria-expanded','false')};menu.addEventListener('click',()=>{const open=links.classList.toggle('open');menu.setAttribute('aria-expanded',open)});links.addEventListener('click',e=>{if(e.target.closest('a'))closeMenu()});document.addEventListener('keydown',e=>{if(e.key==='Escape')closeMenu()})}
const reveals=document.querySelectorAll('.reveal');if('IntersectionObserver' in window){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.12});reveals.forEach(e=>io.observe(e))}else reveals.forEach(e=>e.classList.add('visible'));
const form=document.querySelector('#project-form');if(form){form.addEventListener('submit',e=>{e.preventDefault();let valid=true;form.querySelectorAll('[required]').forEach(input=>{const err=input.parentElement.querySelector('.error');let message='';if(!input.value.trim())message='Required';else if(input.type==='email'&&!input.validity.valid)message='Enter a valid email';if(message){err.textContent=message;input.setAttribute('aria-invalid','true');valid=false}else{err.textContent='';input.removeAttribute('aria-invalid')}});if(!valid){form.querySelector('[aria-invalid]')?.focus();return}const button=form.querySelector('button[type=submit]'),status=form.querySelector('.form-status');button.disabled=true;button.textContent='Sending...';status.textContent='Preparing your brief...';setTimeout(()=>{status.textContent='Thanks. Your brief is ready to send. Email hello@scopebd.org if this page is running offline.';button.textContent='Sent';form.reset()},700)})}
